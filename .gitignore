node_modules
.env*
.eve
.vercel
.next
.output
.nitro
dist
.DS_Store
*.tsbuildinfo

# Runtime state — contains the automaton's real private key (wallet.json),
# ledger, memory, and other per-deployment data. Never commit this.
.automaton/
